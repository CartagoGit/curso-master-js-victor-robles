'use strict'

window.addEventListener('load', () => {
    // document.getElementById("rojo").style.background = "red";
    // document.getElementById("azul").style.background = "cyan";
    // document.getElementById("verde").style.background = "green";


    // var i = 0;
    // var acumula = 0;
    // var resul = 0;
    // function media(...todos) {
    //     console.log(todos);
    //     while (i < todos.length) {
    //         acumula += todos[i];
    //         console.log(i, acumula);
    //         i++;
    //     }
    //     resul = acumula / i - 1;
    //     return resul;

    // }

    // console.log("Resultado: ", media(4, 5, 10));


    // var busqueda;
    // function buscarNumero(array, elemento) {

    //     busqueda = array.findIndex(elementos => elementos== elemento);
    //     if (busqueda != -1) {
    //         document.write(`<strong> El número introducido se encuentra en la posición ${busqueda} </strong>`);
    //     }else {
    //         document.write("<strong> El número introducido no es válido </strong>");
    //     }
    // }

    // //Buscar un valor
    // var numeros = [3, 5, 32, 4, 15];
    // var valor = parseInt(prompt("Introduce el número que quieres buscar en el array"));
    // buscarNumero(numeros, valor);

    // var numeros = [];

    // for (var i = 0; i <= 5; i++) {
    //     numeros.push(parseInt(prompt('ingrese un numero: ', 0)));
    // }

    // function mostrarA(elementos) {
    //     document.write('<ul>');
    //     elementos.forEach((elemento) => {
    //         document.write('<li>' + elemento + '</li>');
    //     });
    // } //<----------------------------------

    // document.write('</ul>');
    // mostrarA(numeros);

    // console.log(numeros);

});